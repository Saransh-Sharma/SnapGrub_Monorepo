# Supabase Tests

Phase 0-1 test coverage starts with SQL smoke tests and Edge Function request fixtures.

Run locally after installing the Supabase CLI:

```sh
supabase start
supabase db reset
npm run backend:test:rls
```
